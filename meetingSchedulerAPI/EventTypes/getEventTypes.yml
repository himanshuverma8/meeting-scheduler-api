info:
  name: getEventTypes
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/event-types"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
