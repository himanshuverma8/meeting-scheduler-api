info:
  name: deleteSchedule
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/schedules/:scheduleId"
  params:
    - name: scheduleId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
